<% var boi = user %>
<%- include("templates/header", { user: boi }) %>

<!-- Reuse login page for multiple purposes. Normal login, 
    when user forgot their info, when they submit their email to reset info -->
<%
    switch(forgor) {
        case 'forgor':
            %>
            <%# //asks what info did u forget %>
            <%- include("templates/forgorLogin") %>
            <% 
            break;
        case 'forgorID':
            %>
            <%# //you forgot id %>
            <%- include("templates/forgorType", { forgorType: 'ID' }) %>
            <% 
            break;
        case 'forgorPW':
            %>
            <%# //you forgot password %>
            <%- include("templates/forgorType", { forgorType: 'PW' }) %>
            <% 
            break;
        default:
            %>
            <%# //user just got on login page %>
            <%- include("templates/normalLogin") %>
            <% 
    }
%>
<%- include("templates/footer_before_login") %>
